{
  "id": "haproxy",
  "name": "HAProxy",
  "description": "监控 HAProxy 负载均衡器，包括前端/后端连接数、请求速率、健康检查状态等",
  "category": "middleware",
  "params": [
    {"key": "servers", "label": "Stats URL 列表", "type": "string_array", "required": true, "placeholder": "http://127.0.0.1:1936/haproxy?stats"},
    {"key": "username", "label": "用户名", "type": "string", "required": false},
    {"key": "password", "label": "密码", "type": "password", "required": false},
    {"key": "interval", "label": "采集间隔", "type": "duration", "default": "15s"}
  ],
  "toml_template": "[[instances]]\nservers = [{{range $i, $s := .servers}}{{if $i}}, {{end}}\"{{$s}}\"{{end}}]\n{{- if .username}}\nusername = \"{{.username}}\"\n{{- end}}\n{{- if .password}}\npassword = \"{{.password}}\"\n{{- end}}\ninterval = \"{{.interval}}\"\n"
}
